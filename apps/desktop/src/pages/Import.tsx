import { ImportWizard } from '../components/domain/ImportWizard';

export const Import = (): JSX.Element => {
  return (
    <section>
      <h2>Import</h2>
      <ImportWizard />
    </section>
  );
};
