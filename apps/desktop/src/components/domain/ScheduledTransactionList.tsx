export const ScheduledTransactionList = (): JSX.Element => {
  // TODO(impl): check overdue scheduled transactions and trigger notifications with auto-post/manual review flows.
  return <section>Scheduled Transactions</section>;
};
