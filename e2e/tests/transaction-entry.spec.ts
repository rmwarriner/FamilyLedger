import { test } from '@playwright/test';

test.skip('Transaction entry supports split reconciliation and keyboard save shortcut', async () => {
  // TODO(impl): test Cmd/Ctrl+Enter save and split imbalance inline validation behavior.
});
