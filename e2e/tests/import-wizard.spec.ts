import { test } from '@playwright/test';

test.skip('Import wizard handles source selection, preview, mapping, dedup review, and confirmation', async () => {
  // TODO(impl): validate each wizard step with recoverable failure states.
});
