import { test } from '@playwright/test';

test.skip('Vault unlock flow validates passphrase and opens dashboard', async () => {
  // TODO(impl): exercise vault creation/unlock flow and validate error recovery messaging.
});
