import { describe, expect, it } from 'vitest';

describe('gnucash-sqlite importer', () => {
  it('TODO(impl): parse fixture and map into ImportResult', () => {
    expect(true).toBe(false);
  });
});
