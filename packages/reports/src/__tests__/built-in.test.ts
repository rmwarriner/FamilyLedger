import { describe, expect, it } from 'vitest';

describe('built-in reports', () => {
  it('TODO(impl): validate built-in report output shape and totals', () => {
    expect(true).toBe(false);
  });
});
