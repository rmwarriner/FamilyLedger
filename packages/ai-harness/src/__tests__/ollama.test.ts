import { describe, expect, it } from 'vitest';

describe('ollama provider', () => {
  it('TODO(impl): verify chat and stream contract with local Ollama service', () => {
    expect(true).toBe(false);
  });
});
