import { describe, expect, it } from 'vitest';

describe('openai provider', () => {
  it('TODO(impl): verify chat and stream contract with OpenAI API', () => {
    expect(true).toBe(false);
  });
});
